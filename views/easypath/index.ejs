<!DOCTYPE html>
<html>

<head>
  <% include ../partial/head.ejs %>
  <!-- <link rel="stylesheet" type="text/css" href="/css/post_index.css"> -->
  <style>
    .search {
      display: flex;
      flex-direction: row;
      justify-content: center;
      align-items: center;
    }
  </style>

</head>

<body>
    <% include ../partial/header.ejs %>
    <div class="container post post-index">
      <br><br><br><br><br><br>
      <h2>Easypath</h2>
      <h5>거인의 발자취는 여기서부터 시작된다.</h5>
      <hr>
      <label>검색</label>
      <div class="container">
        <form action="/easypath/posts/search" method="get" id="searchAction" name="searchAction">
          <div class="form-group row">
            <select name="searchCategory" form="searchAction">
              <option value="" disabled selected>Search option</option>
              <option value="author">작성자</option>
              <option value="titleBody">제목+내용</option>
            </select>

            <div class="col">
              <input type="text" class="form-control search_word" id="searchWord" name="searchWord" placeholder="검색어를 입력하세요">
            </div>
            <div class="col">
              <button type="submit" class="btn btn-primary" onclick="searchContent();">검색</button>
            </div>
          </div>
        </form>
      </div>
    <br>
      <hr>
      <br>
      <div class="row">
        <% easypath.forEach(function(e) { %>
          <div class="card" style="width: 18rem; margin: 2rem;">
            <div class="card-body">
              <h5 class="card-title"><%= e.easypath_id %> </h5>
              <h6 class="card-subtitle mb-2 text-muted"><%= e.easypath_title %></h6>
              <p class="card-text"><%= e.easypath_content %></p>
              <a href="/easypath/show/<%= e.easypath_id %>" class="card-link">이지패스를 보자</a>
              <br>
              <a class="card-link"><%= e.created_time %></a>
            </div>
          </div>
        <% }) %>
      </div>

            <script>
              function searchContent() {
                if ($('#searchWord').val == '') {
                  alert("검색어를 입력해주세요!!!");
                } else {
                  $('#searchAction').submit();
                }
              }
            </script>

            <script>
              $(document).ready(function() {
                $('select').formSelect();
              });
            </script>


    </div>
    <!-- container end -->
</body>

</html>
